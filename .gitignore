.env
.env.*
!.env.example
node_modules/
.venv/
venv/
__pycache__/
*.py[cod]
dist/
build/
coverage/
.pytest_cache/
.mypy_cache/
.ruff_cache/
work/
artifacts/

# ความจำโซนข้ามวันที่รอบผลิตเขียนเอง (zone_memory) — เป็น state ของเครื่อง ไม่ใช่ซอร์ส
# ห้าม commit: ถ้าเข้า git ความจำโซนของเครื่องหนึ่งจะไปทับอีกเครื่องตอน checkout
# ระดับที่ล็อกไว้จะเพี้ยนโดยไม่มีใครเห็น · หายได้ไม่เสียหาย (คำนวณสดแทน บทไม่ตก)
state/
*.log
*.tmp
Thumbs.db
.DS_Store
