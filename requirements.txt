# P002 — ไลบรารีที่ระบบต้องใช้ (Python 3.11+)
# ชั้นตรวจข้อมูลและ level engine (market_calendar, candles, gap_detector, indicators,
# pivots, levels, license_gate, publication_gate, public_copy_validator) ใช้ stdlib ล้วน
matplotlib>=3.8     # กราฟรุ่นใหม่ tools/chart_renderer.py
pillow>=10.0        # กราฟรุ่นเดิม tools/pilot_generator.py (ยังใช้กับชุด baseline v2)
jsonschema>=4.26    # quality evaluation/summary schemas (tested with 4.26.0)

# แหล่งข้อมูลราคาคือ WCB series API (tools/wcb_series_source.py) ซึ่งใช้ stdlib ล้วน
# ไม่ต้องติดตั้งอะไรเพิ่ม ไม่ต้องใช้รหัสเข้าถึง และรันได้ทุกระบบปฏิบัติการ
# (MetaTrader5 ถูกถอดออกทั้งระบบเมื่อ 2026-08-05 ตามคำสั่งผู้ใช้ — ไม่มี dependency
#  ที่ผูกกับแพลตฟอร์มเหลืออยู่แล้ว repo นี้จึงรันบน Linux/macOS ได้ด้วย)

# เครื่องมือพัฒนา
pytest>=8.0         # ชุดเทส tests/
